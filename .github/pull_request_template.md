## What this PR adds

- [ ] New regex patterns in `src/lib/redFlagLexicon.ts` (or wiring in `web3RedFlags.ts`)

## Example phrases that should match

<!-- Paste 1–3 example sentences -->

## Notes

<!-- Optional: false positives to avoid, category choice, etc. -->

## Checklist

- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] Patterns are English-only and not overly broad (see CONTRIBUTING.md)
